extends: jpeg640_fusion_local_weighted_val_syncbn.yaml
paths:
  run_name: jpeg640_fusion_local_weighted_val_syncbn_18ep_full_train
train:
  # Fifteen sampled epochs, then three complete shuffled passes without replacement.
  epochs: 18
  epoch_size: 24000
  full_train_epochs: 3
augmentation:
  final_full_frame_epochs: 3
