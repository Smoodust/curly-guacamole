extends: baseline.yaml
run_name: baseline_long
train:
  # 15 sampled epochs, then 3 complete shuffled passes without replacement.
  epochs: 18
  full_pass_epochs: 3
augmentation:
  final_full_frame_epochs: 3
